	<form action="/pizzas/update/<%=pizzas.id%>" method="POST" class="edit">
	<div class="form-group">
		<h2> editing a pizzas... </h2>
		<% if(flash && flash.err) { %>
<ul class="alert alert-success">
<% Object.keys(flash.err).forEach(function(error) { %>
<li><%- JSON.stringify(flash.err[error]) %></li>
<% }) %>
</ul>
<% } %>
		
	<div class="form-group" style="width: 400px;">
	<label>name</label>
	<input value="<%= pizzas.name %>" name="name" type="text" class="form-control"/>
	</div>

	<div class="form-group" style="width: 400px;">
	<label>name</label>
	<input value="<%= pizzas.desc %>" name="desc" type="text" class="form-control"/>
	</div>

		</div>
		<input type="submit" value="Update Pizzas" class="btn btn-default"/>
		<a href="/pizzas/show/<%=pizzas.id %>" class="btn btn-danger">Cancel</a>
	</form>
	
	